# 007DataDestroyer — nicht versionieren:

# Laufzeit-Zustand & Secrets (werden zur Laufzeit erzeugt)
public/storage/config.json
public/storage/state.json
public/storage/destroyer.log
public/storage/php_error.log
public/storage/*.tmp*

# Die zu löschenden Nutzdaten selbst — nur Schutzdateien behalten
public/data/*
!public/data/.htaccess
!public/data/.gitkeep

# Betriebssystem / Editor
.DS_Store
Thumbs.db
*.swp

# Interne Arbeitsdokumente — gehoeren nicht ins oeffentliche Repo
# (Hausregel: alle uebrigen 10 Repos halten sie ebenfalls draussen)
007datadestroyer_funktionen.txt
*_design.md
PLAN.md
